# 🔐 Система авторизации для сайта

## Описание

Система защиты сайта с помощью страницы входа через логин и пароль. Реализована на чистом HTML, CSS и JavaScript без использования серверной части.

## Файлы системы

- **login.html** - страница входа с формой авторизации
- **index.html** - защищенная главная страница с контентом
- **auth.js** - JavaScript модуль для управления авторизацией
- **styles.css** - стили для всех страниц
- **test-auth.html** - страница для тестирования системы авторизации

## Как использовать

### 1. Открытие сайта

Откройте файл `login.html` в браузере:
```bash
# В Linux/Mac
xdg-open login.html
# или
open login.html

# В Windows
start login.html
```

### 2. Вход в систему

**Учетные данные по умолчанию:**
- Логин: `admin`
- Пароль: `password`

1. Введите логин и пароль на странице входа
2. Нажмите кнопку "Войти"
3. При успешной авторизации вы будете перенаправлены на главную страницу

### 3. Защита страниц

- При попытке открыть `index.html` без авторизации вы будете автоматически перенаправлены на `login.html`
- Сессия сохраняется в localStorage браузера
- Для выхода нажмите кнопку "Выйти" в шапке главной страницы

## Изменение учетных данных

Чтобы изменить логин и пароль, отредактируйте файл `auth.js`:

```javascript
const CREDENTIALS = {
    username: 'ваш_логин',
    password: 'ваш_пароль'
};
```

## Тестирование

Откройте файл `test-auth.html` для интерактивного тестирования системы авторизации:

```bash
# Откройте в браузере
open test-auth.html
```

На странице тестирования вы можете:
- Проверить текущий статус авторизации
- Выполнить тест входа
- Выполнить тест выхода
- Перейти на защищенные страницы

## Функции системы

### auth.js предоставляет следующие функции:

- `login(username, password)` - вход в систему
- `logout()` - выход из системы
- `isAuthenticated()` - проверка авторизации
- `requireAuth()` - защита страницы (перенаправление на login.html)
- `getLoginTime()` - получение времени входа

## Безопасность

⚠️ **Важно:** Эта система использует клиентскую авторизацию и подходит только для базовой защиты контента. Для серьезных проектов рекомендуется использовать серверную авторизацию с шифрованием паролей.

### Ограничения:

- Пароли хранятся в открытом виде в JavaScript
- Нет защиты от перебора паролей
- Сессия хранится в localStorage (может быть изменена через консоль браузера)
- Подходит для защиты от случайного доступа, но не от целенаправленного взлома

## Улучшения для продакшена

Для использования в реальных проектах рекомендуется:

1. Использовать серверную авторизацию (Node.js, PHP, Python и т.д.)
2. Хранить пароли в зашифрованном виде (bcrypt, argon2)
3. Использовать JWT токены или сессии на сервере
4. Добавить HTTPS для защиты передачи данных
5. Реализовать защиту от CSRF атак
6. Добавить ограничение попыток входа
7. Использовать двухфакторную аутентификацию

## Структура проекта

```
/vercel/sandbox/
├── login.html          # Страница входа
├── index.html          # Защищенная главная страница
├── auth.js             # Модуль авторизации
├── styles.css          # Стили
├── test-auth.html      # Страница тестирования
├── img/                # Изображения
└── AUTH_README.md      # Эта инструкция
```

## Примеры использования

### Защита новой страницы

Чтобы защитить новую страницу, добавьте в её HTML:

```html
<!DOCTYPE html>
<html>
<head>
    <title>Защищенная страница</title>
    <link rel="stylesheet" href="styles.css">
</head>
<body>
    <!-- Ваш контент -->
    
    <script src="auth.js"></script>
    <script>
        // Защита страницы
        requireAuth();
    </script>
</body>
</html>
```

### Добавление кнопки выхода

```html
<button onclick="logout()">Выйти</button>
```

### Проверка авторизации в JavaScript

```javascript
if (isAuthenticated()) {
    console.log('Пользователь авторизован');
    console.log('Время входа:', getLoginTime());
} else {
    console.log('Пользователь не авторизован');
}
```

## Поддержка браузеров

Система работает во всех современных браузерах, поддерживающих:
- localStorage API
- ES6 JavaScript
- CSS3

Протестировано в:
- Chrome 90+
- Firefox 88+
- Safari 14+
- Edge 90+

## Лицензия

Свободное использование для любых целей.

## Автор

Создано для защиты сайта с домашним заданием по Git.
